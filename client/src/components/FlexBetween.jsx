const { Box } = require('@mui/material');
const { styled } = require('@mui/system');

export default flexBetween = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
});
